<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Agency Registration</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: "Poppins", sans-serif;
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(135deg, #3306c4, #cbcdd0);
        }
        .container {
            width: 100%;
            max-width: 500px;
            padding: 0 20px;
        }
        .form-card {
            background: white;
            padding: 20px 30px;
            border-radius: 10px;
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        h2 {
            color: #333;
            margin-bottom: 15px;
        }

        .error-message {
            color: red;
            font-size: 14px;
            margin-bottom: 10px;
        }

        label {
            display: block;
            text-align: left;
            font-weight: bold;
            margin: 10px 0 5px;
            color: #444;
        }

        input {
            width: 100%;
            padding: 10px;
            border: 1px solid #ced4da;
            border-radius: 5px;
            margin-bottom: 10px;
        }

        .btn {
            width: 100%;
            background: #007bff;
            color: white;
            border: none;
            padding: 10px;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 10px;
            transition: background 0.3s;
        }

        .btn:hover {
            background: #0056b3;
        }

        .already-registered {
            margin-top: 10px;
            font-size: 13px;
        }

        .already-registered a {
            color: #007bff;
            text-decoration: none;
        }

        .already-registered a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="form-card">
            <h2>Agency Registration</h2>
            
            <% if (error) { %>
                <p class="error-message"><%= error %></p>
            <% } %>

            <form action="/addnewagent" method="POST">
                <label for="agencyName">Agency Name</label>
                <input type="text" id="agencyName" name="agencyName" placeholder="Enter agency name" required value="<%= agencyName || '' %>">

                <label for="officialEmail">Official Email</label>
                <input type="email" id="officialEmail" name="officialEmail" placeholder="Enter official email" required value="<%= officialEmail || '' %>">

                <label for="password">Password</label>
                <input type="password" id="password" name="password" placeholder="Enter password" required>

                <label for="phone">Phone Number</label>
                <input type="tel" id="phone" name="phone" placeholder="Enter phone number" required value="<%= phone || '' %>">

                <label for="address">Address</label>
                <input type="text" id="address" name="address" placeholder="Enter address" required value="<%= address || '' %>">

                <button type="submit" class="btn">Register</button>
            </form>

            <p class="already-registered">Already registered? <a href="/loginformforagency">Login here</a></p>
        </div>
    </div>
</body>
</html>
